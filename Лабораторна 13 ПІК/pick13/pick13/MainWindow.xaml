<Window x:Class="pick13.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pick13"
        mc:Ignorable="d"
       Title="Image Drag Viewer" Height="600" Width="800"
        AllowDrop="True" Drop="Window_Drop"
        Background="#FF2D2D30">

    <Grid>
        <!-- Background Grid -->
        <Grid Background="#FF1E1E1E" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FF007ACC" Padding="10" CornerRadius="3,3,0,0">
                <TextBlock Text="Image Drag &amp; Drop Viewer" 
                          Foreground="White" FontSize="16" FontWeight="Bold"
                          HorizontalAlignment="Center"/>
            </Border>

            <!-- Main Content Area -->
            <Border Grid.Row="1" Background="#FF333337" Margin="5" CornerRadius="3">
                <ScrollViewer x:Name="scrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <Canvas x:Name="imageCanvas" Background="Transparent">
                        <Image x:Name="displayedImage" 
                               MouseLeftButtonDown="Image_MouseLeftButtonDown"
                               MouseMove="Image_MouseMove"
                               MouseLeftButtonUp="Image_MouseLeftButtonUp"
                               Stretch="Uniform"
                               Cursor="Hand"
                               RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <TranslateTransform x:Name="dragTransform"/>
                            </Image.RenderTransform>
                        </Image>
                    </Canvas>
                </ScrollViewer>
            </Border>

            <!-- Footer Controls -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button x:Name="btnLoadImage" Content="Load Image" 
                        Click="BtnLoadImage_Click" Margin="5" Padding="10,5"
                        Background="#FF007ACC" Foreground="White"/>
                <Button x:Name="btnResetPosition" Content="Reset Position" 
                        Click="BtnResetPosition_Click" Margin="5" Padding="10,5"
                        Background="#FF555555" Foreground="White"/>
                <Button x:Name="btnFitToScreen" Content="Fit to Screen" 
                        Click="BtnFitToScreen_Click" Margin="5" Padding="10,5"
                        Background="#FF555555" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Drop Hint -->
        <Border x:Name="dropHint" Background="#66007ACC" BorderBrush="#FF007ACC" 
                BorderThickness="2" CornerRadius="5" Visibility="Collapsed"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20">
            <TextBlock Text="Drop image here" Foreground="White" FontSize="20"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Status Bar -->
        <StatusBar VerticalAlignment="Bottom" Background="#FF007ACC" Foreground="White">
            <StatusBarItem>
                <TextBlock x:Name="statusText" Text="Ready to load image..."/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="positionText" Text="Position: 0, 0" Margin="10,0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

